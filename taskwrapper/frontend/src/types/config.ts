// Import the generated types from Wails
import { main } from '../../wailsjs/go/models';

// Re-export the generated types for convenience
export type Config = main.Config;
export type Repository = main.Repository;
export type RepositoryInfo = main.RepositoryInfo;